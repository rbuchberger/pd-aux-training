<%= form_for @timecard do |f| %>
  <div class="row justify-content-center my-3">
    <div class="col-xl-5">
      <div class="form-group form-inline">
        <%= f.label "Clock in:", class: "mr-auto" %>
        <%= f.date_field :field_clock_in_date, value: @timecard.field_clock_in_date, class: "form-control mx-1" %>
        <%= f.time_select :field_clock_in_time, {ampm: true, minute_step: 15}, class: "form-control mx-1" %>
      </div>
    </div>
  </div>
  <div class="row justify-content-center">
    <div class="col-xl-5">
      <div class="form-group form-inline">
        <%= f.label "Clock out:", class: "mr-auto" %>
        <%= f.time_select :field_clock_out_time, {ampm: true, minute_step: 15}, class: "form-control mx-1" %>
      </div>
    </div>
  </div>
  <div class="row justify-content-center">
    <div class="form-group col-xl-5 mx-1 mt-4 mb-0">
      <%= f.label "Description:" %>
      <%= f.text_area :description, class: "form-control", rows: "3" %><br>
    </div>
  </div>
  <div class="row justify-content-center">
    <div class="col-xl-5 mb-1">
      <%= f.submit "Log it!", class: "btn btn-success btn-block mb-3" %>
      <p>Timecards cannot be shorter than 30 minutes, or longer than 24 hours.</p>
      <p>All timecards must have <i>something</i> in the description.</p>
      <p>If you worked past midnight, use the date you started work and just plug the times in; it'll work out the end date using common sense.</p>
      <p>You can now log subsequent timecards, so for example you could have one from 0800-1200, and another from 1200-1600 that same day. (The old version would reject this as overlapping with an existing timecard)</p>
    </div>
  </div>
<% end %>
