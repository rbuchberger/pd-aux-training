<div class="card">
  <div class="card-header">
    <h6><%= document.name %></h6>
    <small class="text-muted"><%= document.updated_at.strftime("%b %d, %Y") %></small>
  </div>
  <div class="card-body document-grid">
    <div class="document-description">
      <p class="card-text"><%= render inline: markdown.render(document.description) %></p>
    </div>
    <div class="document-download">
      <p>
        <%= link_to "Edit Details", edit_document_path(document), class: "mr-3" if policy(document).edit? %>
        <%= link_to 'extension name', '#', class: "btn btn-sm btn-primary" %>
      </p>
      <p>
        <small class='text-muted'><%= number_to_human_size(999) %></small>
      </p>
    </div>
  </div>
</div>
